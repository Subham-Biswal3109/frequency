pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Only needed once the MySQL connection is fixed (not required for
# train_model.py / evaluate_model.py, which read from CSV):
sqlalchemy>=2.0
pymysql>=1.1
